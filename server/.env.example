ANTHROPIC_API_KEY=
PORT=3001

# OAuth 2.0 Client ID from Google Cloud Console (Credentials → OAuth client ID →
# Web application). Same value the web app uses as VITE_GOOGLE_CLIENT_ID below.
GOOGLE_CLIENT_ID=

# Random secret used to sign our own session cookie. Generate with:
#   openssl rand -hex 32
SESSION_SECRET=
